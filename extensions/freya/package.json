{
  "name": "freya",
  "displayName": "Freya",
  "description": "Tungsten's built-in coding agent. Local-first, BYOK, no telemetry.",
  "version": "0.1.0",
  "publisher": "tungsten",
  "license": "MIT",
  "engines": {
    "vscode": "^1.131.0"
  },
  "categories": [
    "AI",
    "Chat"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished",
    "onLanguageModelChatProvider:freya"
  ],
  "enabledApiProposals": [
    "defaultChatParticipant",
    "chatProvider"
  ],
  "capabilities": {
    "virtualWorkspaces": false,
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "The light lane (autocomplete, commit messages, explanations) runs on the embedded local model, only reads, and stays on. The agent writes files and can run commands, so it is paused until you trust the folder.",
      "restrictedConfigurations": [
        "freya.local.runtimePath",
        "freya.local.port",
        "freya.ollama.url"
      ]
    }
  },
  "contributes": {
    "chatParticipants": [
      {
        "id": "tungsten.freya",
        "name": "freya",
        "fullName": "Freya",
        "description": "What should Freya do?",
        "isDefault": true,
        "modes": [
          "agent",
          "ask"
        ]
      }
    ],
    "commands": [
      {
        "command": "freya.setKeys",
        "title": "Set Cloudflare keys",
        "category": "Freya"
      },
      {
        "command": "freya.clearKeys",
        "title": "Delete stored keys",
        "category": "Freya"
      },
      {
        "command": "freya.showBackend",
        "title": "Show which model is in use",
        "category": "Freya"
      },
      {
        "command": "freya.checkOllama",
        "title": "Check that Ollama and the models are present",
        "category": "Freya"
      },
      {
        "command": "freya.generateCommitMessage",
        "title": "Write commit message",
        "category": "Freya",
        "icon": "$(sparkle)"
      },
      {
        "command": "freya.scanStagedForSecrets",
        "title": "Scan staged changes for secrets",
        "category": "Freya"
      },
      {
        "command": "freya.explainSelection",
        "title": "Explain selected code",
        "category": "Freya"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "freya.generateCommitMessage",
          "when": "scmProvider == git",
          "group": "navigation"
        }
      ]
    },
    "walkthroughs": [
      {
        "id": "freya.setup",
        "title": "Get started with Freya",
        "description": "Freya has its own local model built in. Nothing to install.",
        "steps": [
          {
            "id": "freya.setup.ollama",
            "title": "Everything light already runs locally",
            "description": "Autocomplete, commit messages and code explanations run on a 1.5B model that ships with the app. No installation, no sign-in, no network traffic.\n[Explain selected code](command:freya.explainSelection)",
            "media": {
              "markdown": "media/walkthrough-ollama.md"
            }
          },
          {
            "id": "freya.setup.models",
            "title": "Heavier work: cloud or your own large model",
            "description": "Agent work across several files wants a larger model. Either Cloudflare Workers AI with your own keys, or your own Ollama if you have the hardware.\n[Set Cloudflare keys](command:freya.setKeys)",
            "media": {
              "markdown": "media/walkthrough-models.md"
            }
          },
          {
            "id": "freya.setup.chat",
            "title": "Open the chat",
            "description": "Freya is the default participant in the chat panel.\n[Open the chat](command:workbench.action.chat.open)",
            "media": {
              "markdown": "media/walkthrough-chat.md"
            }
          }
        ]
      }
    ],
    "configuration": {
      "title": "Freya",
      "properties": {
        "freya.chat.backend": {
          "type": "string",
          "enum": [
            "auto",
            "workersai",
            "ollama"
          ],
          "enumDescriptions": [
            "Decide automatically: cloud if Cloudflare keys exist, otherwise your local Ollama.",
            "Cloudflare Workers AI. Requires your own keys (BYOK).",
            "Local Ollama. Optional extra; requires that you pulled a large model."
          ],
          "default": "auto",
          "description": "HEAVY lane: which model Freya's chat/agent uses. The light lane is controlled by freya.light.backend."
        },
        "freya.light.backend": {
          "type": "string",
          "enum": [
            "embedded",
            "ollama"
          ],
          "enumDescriptions": [
            "The embedded 1.5B model that ships with the app. Requires no installation.",
            "Your own Ollama. Use this if you would rather run a different small model."
          ],
          "default": "embedded",
          "description": "LIGHT lane: autocomplete, commit messages and code explanations."
        },
        "freya.chat.workersAiModel": {
          "type": "string",
          "default": "@cf/qwen/qwen3-30b-a3b-fp8",
          "description": "Model ID on Workers AI."
        },
        "freya.chat.ollamaModel": {
          "type": "string",
          "default": "qwen2.5-coder:14b",
          "description": "Ollama model for chat/agent."
        },
        "freya.local.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Use the embedded local model (llama.cpp + Qwen2.5-Coder-1.5B) for the light tasks. Off = fall back to Ollama."
        },
        "freya.local.port": {
          "type": "number",
          "default": 11435,
          "description": "Port for the embedded llama.cpp server. 11434 belongs to Ollama and is never used."
        },
        "freya.local.contextSize": {
          "type": "number",
          "default": 4096,
          "description": "Context window for the embedded model."
        },
        "freya.local.runtimePath": {
          "type": "string",
          "default": "",
          "description": "Custom path to the freya-runtime folder. Empty = look in the app's resources."
        },
        "freya.secrets.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Warn about secrets on paste and before commit. Runs entirely locally (pattern matching, no model)."
        },
        "freya.commit.model": {
          "type": "string",
          "default": "",
          "description": "Ollama model that writes commit messages. Empty = same as freya.chat.ollamaModel. Must be an instruct model, not a base model."
        },
        "freya.chat.maxSteps": {
          "type": "number",
          "default": 25,
          "minimum": 1,
          "maximum": 100,
          "description": "Maximum number of tool steps per question."
        },
        "freya.ollama.url": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "Address of Ollama. Used by both chat and autocomplete."
        },
        "freya.autocomplete.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Inline completion from a local FIM model."
        },
        "freya.autocomplete.model": {
          "type": "string",
          "default": "qwen2.5-coder:1.5b-base",
          "description": "FIM model in Ollama. Must be a base model, not an instruct model."
        },
        "freya.autocomplete.debounceMs": {
          "type": "number",
          "default": 200,
          "description": "Delay after the last keystroke before a suggestion is fetched."
        },
        "freya.autocomplete.maxTokens": {
          "type": "number",
          "default": 256,
          "description": "Maximum length of a suggestion (num_predict)."
        },
        "freya.autocomplete.prefixChars": {
          "type": "number",
          "default": 3000,
          "description": "Number of characters before the cursor sent as context."
        },
        "freya.autocomplete.suffixChars": {
          "type": "number",
          "default": 1000,
          "description": "Number of characters after the cursor sent as context."
        }
      }
    },
    "languageModelChatProviders": [
      {
        "vendor": "freya",
        "displayName": "Freya"
      }
    ]
  },
  "scripts": {
    "compile": "gulp compile-extension:freya",
    "watch": "gulp watch-extension:freya"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/microsoft/vscode.git"
  }
}
